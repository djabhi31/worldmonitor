name: Automated Upstream Sync (Lifetime Sync)

on:
  schedule:
    # Runs every day at 04:00 UTC (09:30 AM IST)
    - cron: '0 4 * * *'
  workflow_dispatch: # Allows manual trigger from GitHub Actions tab

permissions:
  contents: write

jobs:
  sync-upstream:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Set up Git user
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Add Upstream Remote & Fetch
        run: |
          git remote add upstream https://github.com/koala73/worldmonitor.git || true
          git config remote.upstream.fetch "+refs/heads/*:refs/remotes/upstream/*"
          git fetch upstream +refs/heads/main:refs/remotes/upstream/main

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Run Upstream Sync Engine
        run: |
          node scripts/sync-upstream.mjs

      - name: Push Synced Updates to Origin Main
        run: |
          git push origin main || echo "Nothing new to push."
